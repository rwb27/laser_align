// Config file for tiled image generating function. Remaining parameters defined in microscope_defaults.json.

{
    "filename":         "align.hdf5",
    "n_steps":                [  // The number of steps per axis to take, i.e. generates an n+1 x n+1 images in total.
                               [10, 300],
                               [10, 50]],
    "parabola_N":           10,
    "parabola_step":        25
}